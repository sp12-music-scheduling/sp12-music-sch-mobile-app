import React from 'react';
import {
  StyleSheet,
  Text,
  View,
} from 'react-native';

const StudentVideosHomePage = ({navigation}) => {
  return (
      <View style={styles.container}>
          <Text>Coming Soon!</Text>

      </View>
  )
};

export default StudentVideosHomePage;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#ffffff'
  },
});
 

 